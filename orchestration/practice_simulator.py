# TODO (Role 4 — Orchestration Engineer) — build step 4
#
# Deterministic, seeded simulator of practice outcomes. Given a lesson id and
# a seed, returns a concrete outcome with some friction (not a clean success)
# for an honest student to write into student_practice_log. A bluffer skips
# calling this, so the log stays empty for that lesson.
