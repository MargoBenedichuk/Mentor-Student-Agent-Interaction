# Final Submission — Mentor–Student Multi-Agent Simulation

> Owner: Role 6 (Editor/Writer). Assemble incrementally as other roles finish, not all at once at the end.

## 1. Mentor Prompt

TODO: paste the final `prompts/mentor/current.txt`.

## 2. Student Prompt

TODO: paste the final `prompts/student/current.txt`.

## 3. Tools

TODO: models used (mentor/student/judge = `gpt-4o-mini`), orchestration approach (two mirrored contexts),
tool list (`ledger_read/write`, `practice_read/write`, `practice_simulator`, `advance_decision`, `judge`),
and why each one exists.

## 4. Full Transcript

TODO: paste the transcript of the best run (`courses/prompt-engineering/logs/run_NNN/transcript.txt`).

## 5. Reflection

TODO: what broke, prompt version history, where the mentor still gets fooled by the bluffer.
Build this section after each failed run, not just at the end.
